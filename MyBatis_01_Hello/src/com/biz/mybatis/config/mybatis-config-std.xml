<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
  
  <configuration>
  	<typeAliases>
  		<typeAlias type=""/>
  	</typeAliases>
  	<!-- environments Mybatis에서 JDBC를 사용한 Connection들을 생성하고 관리할 수 있도록 기본 설정값을 
		지정하는 곳 -->
  	<environments default="">
  			<!-- environment는 여러개를 지정해두고 필요에 따라 선택해서 사용가능 -->
  		<environment id="">
 			 <transactionManager type="">
			  		  </transactionManager>
				<dataSource type="">
				</dataSource>  
		  </environment>
  	</environments>  
  </configuration>